{% extends 'base.html.twig' %}

{% block title %}Modifier Tâche{% endblock %}

{% block body %}
    <h1>Modifier Tâche</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('app_tache') }}">Retour à la liste des tâches</a>
{% endblock %}
